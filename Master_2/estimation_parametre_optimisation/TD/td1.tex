\documentclass[12pt,a4paper]{article}

\makeatletter
\def\input@path{{/home/maenwe/Master_CSM/modèle_cours_latex}}
\makeatother

\usepackage{prise_de_note}
\graphicspath{{images}}
\begin{document}
	\pagenumbering{gobble}
	\begin{titlepage}
		\centering
		
		% Logo
		\begin{figure}[h]
			\centering
			% Ajustez la largeur si besoin (ex: 0.4 ou 0.6)
			\includegraphics[width=0.5\textwidth]{/home/maenwe/Master_CSM/modèle_cours_latex/logo.jpg} 
		\end{figure}
		
		\vspace{2cm}
		
		% Filet horizontal du haut
		\rule{\linewidth}{0.5mm} \\[0.4cm]
		
		% Titre
		{\color{bleuTitre} \Huge \bfseries Estimation de paramètres et optimisations} \\[0.4cm]
		
		% Filet horizontal du bas
		\rule{\linewidth}{0.5mm}
		
		\vspace{1.5cm}
		
		{\Large \textbf{TD1}}
		
		\vfill
		
		% Bloc Informations
		\begin{minipage}{0.45\textwidth}
			\begin{flushleft} \large
				\textbf{Enseignants :} \\
				M. Abdelaziz \textsc{BELMILOUDI}
			\end{flushleft}
		\end{minipage}
		\hfill
		\begin{minipage}{0.45\textwidth}
			\begin{flushright} \large
				\textbf{Étudiant :} \\
				Nathan \textsc{HASCOET}
			\end{flushright}
		\end{minipage}
		
		\vspace{2cm}
		
		% Date
		{\large \today}
		
	\end{titlepage}
	
	\newpage
	\pagenumbering{arabic}
	\setcounter{page}{1}
	

	\begin{sol}
		$ $\\
		\begin{minipage}[t]{17cm}
			Soit $x \not = y \in X$ deux solutions du problème suivant : $\underset{x \in X}\min f(x)$. On a :\\
			\[
				f(x+\lambda (y-x)) < f(x) + \lambda(f(y)-f(x)),
			\]
			of $x+\lambda(y-x) \in X$, donc $f(x+\lambda(y-x)) \geq f(x),f(y)$.\\
			Ainsi $ f(x+\lambda(y-x)) < f(x) + \lambda(f(y)-f(x)) \leq f(x) + \lambda(f(x)-f(x)) = f(x)$, ce qui est absurde.
		\end{minipage}
	\end{sol}
	
	\begin{sol}
		$ $\\
		\begin{minipage}[t]{17cm}
			\begin{enumerate}
				\item (théorème de projection d'Hilbert)\\
				Soit $(u_n)_n$ une suite telle que $\norme{x-u_n} \longrightarrow \inf_u \norme{x-u}$. Ainsi la suite $(u_n)$ est bornée, en effet $\norme{u_n} \leq \norme{x}+\norme{x-u_n}$. Donc il existe une sous-suite convergente $(v_n)$ convergente, notons $u$ cette limite. De plus puisque la norme au carré est strictement convexe, $u$ est unique.
				
				\item Supposons que $v^\star = \mathrm{Proj}_X(x)$. On a alors : $ \frac12\norme{x-v^\star}^2 \leq \frac12\norme{x-v}^2 $. Donc $\left< \nabla g(v^\star),v-v^\star\right> \geq 0$, ainsi $\left<v^\star - x, v-v^\star\right> \geq 0$.
				
				\item Soit $x_i \not\in X$, on a : $\left<v_i^\star - x_i, v-v_i^\star\right> \geq 0$. Ainsi $\left<v_1^\star - v_2^\star + x_2 - x_1, v_2^\star-v_1^\star\right> \geq 0$. Ainsi avec Cauchy-Schwartz : $\norme{v_1^\star-v_2^\star} \leq \norme{x_2-x_1}\norme{v_1^\star-v_2^\star}$
			\end{enumerate}
		\end{minipage}
	\end{sol}
	
	\begin{sol}
		$ $\\
		\begin{minipage}[t]{17cm}
			\begin{enumerate}
				\item Soit $\bar x$ le projeté orthogonal de $x$ sur $X$, $s = \bar x-x$ et $b=\left< s,v^\star\right>$. Ainsi pour tout $ v \in X$, $\left<s,v\right> = \left< \bar x - x, v\right> \leq \left< s,v^\star\right> = b$. Donc $X \subset H^-$. De plus $ \left<s,x\right> - \left<s,v^\star\right> = -\norme{s}^2 < 0 $. Donc $x \in \mathrm{Int}(H^+)$.
				
				\item D'après ce qui précède $X \subset\bigcap_{X \subset H^-} H^-$. Puisque $x \not \in X$, il existe $H$ tel que $x \in  \mathrm{Int}(H^+)$ et $X \subset H^-$, ainsi $x \not \in H^+$, et donc $x \not \in \bigcap_{X \subset H^-} H^-$. Il suit que si $v \in X$, alors par contraposé du résultat précédent, $v \in \bigcap_{X \subset H^-} H^-$. Finalement $X = \bigcap_{X \subset H^-} H^-$.
			\end{enumerate}
		\end{minipage}
	\end{sol}
	
	\begin{sol}
		$ $\\
			\begin{enumerate}
				\begin{minipage}{17cm}
				\item $f''(x) = e^x \leq 0 \implies$ $f$ est convexe (et donc quasi convexe) mais n'est pas concave et est quasi concave car $(x < y)$ $f(\lambda x + (1-\lambda)y) > f(x) $.
				\item $f''(x) = \frac1{x^2} \geq 0 \implies$ $f$ est convexe (et donc quasi convexe) mais n'est pas concave ni quasi concave.
				\item $f(\lambda (-1) + (1-\lambda)1) = f(\sqrt{1/2}) > f(-1)$, donc $f$ n'est pas quasi convexe (ni quasi concave).
				\end{minipage}
				\newpage
				\begin{minipage}{17cm}
				\item Pour tout $p \in 2\N^\star$, $f$ est convexe et n'est pas concave ni quasi-concave. Soit $p \in 2\N+1$. $f_p$ est donc strictement croissante, donc quasi-concave, elle n'est ni concave ni convexe ni quasi convexe.
				\item 
				\item $\nabla f(x,y) = \begin{bmatrix}
					y\\
					x
				\end{bmatrix}$ et ainsi $\nabla^2 f(x,y) = \begin{bmatrix}
				0 & 1 \\
				1 & 0
				\end{bmatrix}$, donc $f$ n'est ni convexe ni concave car les valeurs propres de la hessienne sont 1 et -1 indépendamment de $x$ et $y$.
				\end{minipage}
			\end{enumerate}
	\end{sol}
	
	\begin{sol}
		$ $\\
		
		\begin{minipage}{17cm}
			On remarque que $C$ est un convexe fermé (c'est même un cône fermé).\\
			Supposons que $g \not\in C$ et montrons que $\mathcal{S}$ est non vide.
			D'après l'exo 3 il existe $y$ tel que $C \subset H_y^+$ et $g \in \mathrm{int}(H_y^-)$. 
		\end{minipage} 
	\end{sol}
\end{document}