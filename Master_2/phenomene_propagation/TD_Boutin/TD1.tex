\documentclass[12pt,a4paper]{article}

\makeatletter
\def\input@path{{/home/maenwe/Master_CSM/modèle_cours_latex}}
\makeatother

\usepackage{prise_de_note}
\graphicspath{{images}}
\begin{document}
	\pagenumbering{gobble}
	\begin{titlepage}
		\centering
		
		% Logo
		\begin{figure}[h]
			\centering
			% Ajustez la largeur si besoin (ex: 0.4 ou 0.6)
			\includegraphics[width=0.5\textwidth]{/home/maenwe/Master_CSM/modèle_cours_latex/logo.jpg} 
		\end{figure}
		
		\vspace{2cm}
		
		% Filet horizontal du haut
		\rule{\linewidth}{0.5mm} \\[0.4cm]
		
		% Titre
		{\color{bleuTitre} \Huge \bfseries Phénomène de propagation} \\[0.4cm]
		
		% Filet horizontal du bas
		\rule{\linewidth}{0.5mm}
		
		\vspace{1.5cm}
		
		{\Large \textbf{Fiche de TD 1}}
		
		\vfill
		
		% Bloc Informations
		\begin{minipage}{0.45\textwidth}
			\begin{flushleft} \large
				\textbf{Enseignant :} \\
				M. Benjamin \textsc{BOUTIN}
			\end{flushleft}
		\end{minipage}
		\hfill
		\begin{minipage}{0.45\textwidth}
			\begin{flushright} \large
				\textbf{Étudiant :} \\
				Nathan \textsc{HASCOET}
			\end{flushright}
		\end{minipage}
		
		\vspace{2cm}
		
		% Date
		{\large \today}
		
	\end{titlepage}
	
	\newpage
	\pagenumbering{arabic}
	\setcounter{page}{1}
	
	\begin{sol}$ $\\
		\begin{enumerate}
			\item 			
		\begin{equation}
			u_1(t,x) = e^{(-x-2t)^2} = u_1(0,x-2t)
		\end{equation}
		\begin{minipage}{16cm}
			On en déduit : $\partial_t u_1(t,x) = -2\partial_x u_1(0,x-2t) = -2\partial_x u_1(t,x),$ et ainsi :
		\end{minipage}
		\begin{equation}
			\partial_t u_1+2\partial u_1 = 0
		\end{equation}
		\textcolor{red}{équation de transport à vitesse 2.}\\
		
		$u_2(t,x) = e^{-(x+3t)^2}$ transport pur à vitesse -3 (sur l'axe $\R$).\\
		
		$u_3(t,x,y) = e^{-(x+y-t)^2} = u_3(0,?)$\\
		$\partial_t u_3 + \underbrace{\mathrm{div}_{(x,y)}(\vec c u_3)}_{\text{avec } \vec c = (c_x, c_y)}$ $  \underset{?}{\textcolor{red}{\longrightarrow}} $ $\partial_t u_3 + c_x\partial_xu_3 + c_y\partial_yu_3 = 0$.\\
		$\vec c = \vec n \times\norme{\vec c}$ avec $\vec n = \dfrac{\vec c}{\norme{\vec c}}$.\\
		$\partial_t u_3(t,x,y) = 2(x+y-t)u_3(t,x,y)$\\
		$\partial_xu_3(t,x,y)=-2(x+y-t)u_3(t,x,y)$\\
		$\hookleftarrow 2\partial_tu_3(t,x,y)+\partial_xu_x+\partial_yu_3 = 0$.\\
		$\begin{cases}
			c_x = 1/2\\
			c_y = 1/2
		\end{cases}$ avec $\norme{\vec c} = 1/\sqrt 2$ et $\vec n = \frac1{\sqrt{2}}\begin{bmatrix}
		1\\ 1
		\end{bmatrix}$.
		
		\item $u_3(t,x,y) = F\underbrace{\left(\vec n\cdot \begin{bmatrix}
			x\\ y
		\end{bmatrix}-\norme{\vec c}t\right)}_{\frac1{\sqrt 2}(x+y-t)}$\\
	
	avec $F(z) = \exp(-2z^2)$\\
	
	\begin{minipage}[t]{20cm}
		$\partial_{tt}u_3 = \partial_t\left(-\norme{\vec c}F'\left(\vec n\cdot \begin{bmatrix}
		x\\ y
	\end{bmatrix}-\norme{\vec c}t\right)\right) = \norme{\vec c}^2F''\left(\vec n\cdot \begin{bmatrix}
	x\\ y
	\end{bmatrix}-\norme{\vec c}t\right)$.\\
	$\partial_{xx}u_3 = \frac12F''\left(\vec n\cdot \begin{bmatrix}
		x\\ y
	\end{bmatrix}-\norme{\vec c}t\right)$\\
	$\partial_{t} u_3 - \frac12 \Delta u_3 = 0 \iff \partial_{t} u_3 - \left(\frac1{\sqrt{2}}\Delta u_3\right) = 0$
	\end{minipage}
	\item $u_3(t,x,y) = \exp(-(x+y-t)^2) = 1$ ssi $x+y=t$.\\
	$u_2(t,x,y) = \alpha$, nécessairement $ \alpha \in ]0,\, 1]$, $\alpha = 1$ déjà traité. Et ainsi $u_2 = \alpha \iff x+y=t\pm \sqrt{\ln \alpha}$.\\
	Lorsque le temps avance de $t$ à $t+T$, la distance entre les deux droites $x+y=t$ et $x+y=t+T$ est, d'après Pythagore, $\frac1{\sqrt 2}T$.
	
	\item $u(t,x,y) = F(ax+by-\gamma t)$ avec $F \in \mathcal C^2(\R^2,\R)$.\\
	Le vecteur normale est : $\vec n = \dfrac{(a,\, b)}{\sqrt{a^2+b^2}}$. Ainsi :
	\[
		ax+by-\gamma t = \sqrt{a^2+b^2}\left[\vec n \cdot (x,\, y) - \dfrac{\gamma}{\sqrt{a^2+b^2}} t\right]
	\]
	
	\begin{minipage}[t]{16cm}
		Propagation du profil dans la direction $\vec n$ à la vitesse $\dfrac{\gamma}{\sqrt{a^2+b^2}} = c$.\\
	$\partial_t u + \dfrac{\gamma}{\sqrt{a^2 + b^2}}\vec n \cdot \nabla_{(x,y)} = 0$ est vérfiée par le calcul des dérivée composées $F\left(\sqrt{a^2+b^2}\left[\vec n \cdot (x,\, y)-ct\right]\right)$,\\ $\partial_t u(t,x,y) = -c\sqrt{a^2+b^2}F'(\cdots)$\\ et $\nabla_{(x,y)} u(t,x,y) = \sqrt{a^2+b^2}\vec n F'(\cdots)$.
	\end{minipage}
	
	\item $u$ est-elle solution de :
	\[
		u_{t} - c^2(u_{xx}+u_{yy}) = 0
	\]
	\begin{minipage}{16cm}
		$\gamma^2F''(ax+by)\gamma t) - c^2(a^2 + b^2)F''(ax+by-\gamma t) = 0$ indép. de $F$. Cette équation est équivalente à :
		
		\[
			\begin{aligned}
				& \gamma^2 = c^2(a^2+b^2) \\
				\iff & \gamma^2 = c^2(a^2+b^2)\\
				\iff & c = \pm \dfrac{\gamma}{\sqrt{a^2+b^2}}
			\end{aligned}
		\]
	\end{minipage}
	
	
	\item  $v(t,x,y) = e^{-(x-2t)^2}e^{-y^2} = F(x-2t)F(y)$.\\
	Cette équation est satisfaite par $v$ : $\partial_t v + 2 \partial_x v = 0$\\
	$v(0,x,y) = e^{-(x^2+y^2)}$, et : $\begin{cases}
		\partial_t v + \vec c \cdot \nabla v = 0\\
		\text{avec } \vec c = \begin{bmatrix}
			2 \\ 0
		\end{bmatrix} = 2\underbrace{\begin{bmatrix}
		1 \\
		0
		\end{bmatrix}}_{unitare}
	\end{cases}$\\
	$v$ est-elle solution de l'équation des ondes 2D : $v_{tt} - c^2 \Delta v = 0$ ?\\
	$\partial_ t v = -2F'(x-2t)F(y)$, $\partial_{tt} v = 4F''(x-2t)F(y)$.\\
	$\partial_{xx} v = F''(x-2t)F(y)$, $\partial_yy v = F(x-2t)F''(y)$.\\
	De plus $F''(z) = (4z^2-2)F(z)$.\\
	$\partial_{tt} v - c^2 \Delta v = \left(4\left[4(x-2t)^2-2\right]-c^2\left[(4(x-2t)^2-2)+(4y^2-2)\right]\right)F(x-2t)F(y)$.\\
	C'est un polynôme (ce qu'il y a entre les parenthèses) en $t,x,y$ \underline{non-identiquement nul}. $v$ ne résout aucune équation des ondes 2D.
		\end{enumerate}
	\end{sol}
	
	\begin{sol}$ $\\
		\begin{enumerate}
			\item Pour $f \in \mathcal C^2(\R_+^\star)$, on vérifie que : $\Delta F(x,y,z) = f_{rr} + \frac2r f_r$, où $F(x,y,z) = f(r)$.\\
			\begin{minipage}[t]{15cm}
				On a : $\partial_x F = \dfrac{\partial r}{\partial x}\dfrac{\partial f}{\partial r} = \dfrac xr f_r$ et ainsi : $\partial_{xx} F = \left(\dfrac1r - \dfrac {x^2}{r^3}\right)f_r + \left(\frac{x}{r}\right)^2f_{rr}$.
			\end{minipage}\\
			Il suite l'équation suivante : $\Delta F = \left(\frac 3r - \frac{r^2}{r^3}\right)f_r + \frac{r^2}{r^2} f_{rr} = f_{rr} + \frac2rf_r$.
		\end{enumerate}
	\end{sol}
	
	\begin{sol}
		content
	\end{sol}
	
	\begin{sol}
		$ $\\
		\begin{enumerate}
			\begin{minipage}{17cm}
			\item $q = \rho u$, $U = \begin{bmatrix}
					p \\
					q
				\end{bmatrix}$, $u = q/\rho$, $\begin{bmatrix}
				p \\
				u
				\end{bmatrix} \mapsto \begin{bmatrix}
				p \\
				\rho u = q
				\end{bmatrix}$ et $\varphi(p,q) = \rho u^2 + P(\rho)$,\\
				or $\rho u^2 + P(\rho) = \rho(q/\rho)^2 + P(\rho) = q^2/\rho + P(\rho) = (\rho u)^2/\rho + P(\rho) $.\\
				
				$f'(U) = \begin{bmatrix}
					\dfrac{\partial (\rho u)}{\partial\rho} & \dfrac{(\rho u)}{\partial (\rho u)}\\
					\dfrac{\partial\left((\rho u)^2/\rho + P(\rho)\right)}{\partial\rho} & \dfrac{\left((\rho u)^2/\rho + P(\rho)\right)}{\partial (\rho u)}
				\end{bmatrix}$ \hspace{1.5cm} $\dfrac{(\rho u^2)}{\partial \rho}\Big|_{\rho u = cst} = $ ?
				
				$f'(U) = \begin{bmatrix}
					0 & 1 \\
					-\dfrac{(\rho u)^2}{\rho^2} + P'(\rho) & 2\rho u /\rho
				\end{bmatrix} = \begin{bmatrix}
				0 & 1 \\
				-u^2 + P'(\rho) & 2u
				\end{bmatrix}$\\
				
				$\begin{cases}
					\partial_t\rho + 1\times\partial_x(\rho u) = 0 \\
					\partial_t (\rho u) + (P'(\rho)-u^2) \partial_x\rho + 2u\partial_x(\rho u) = 0
				\end{cases}$
			\end{minipage}
			
			\item \begin{minipage}[t]{17cm}
				$\rho(t,x) \simeq \rho_{eq}$ et $u(t,x) \simeq 0$, $p = P(\rho) \simeq P(\rho_{eq}) + P'(\rho_{eq})(\rho-\rho_{eq})$.\\
				
			$\rho = \rho_{eq} + \underbrace{(\rho-\rho_{eq})}_{\delta_{eq}}$, $\partial_t \rho + \partial_x (\rho u) = \partial_t(\delta \rho) + \partial_x(\rho_{eq} u) + \underbrace{\partial_x(\delta\rho\times u)}_{\text{ordre 2}} = \partial_x (\delta \rho) + \rho_{eq} \partial_x u$\\
			$\partial_t(\rho_eq u)+\left(P'(\rho_{eq})+P''(\rho_{eq})\cancel{(\rho-\rho_{eq})} - \cancel{u^2} \right)\partial_x(\delta \rho) + 2u\cancel{\partial_x(\rho_{eq} u )} + 2u\cancel{\partial_x(\delta \rho u)} = 0$\\
			
			$\begin{cases}
				\partial_t(\delta \rho)+\rho_{eq}\partial_x u = 0\\
				\rho_{eq}\partial_t u + P'(\rho_{eq})\partial_x( \delta \rho) = 0
			\end{cases}$\\
			avec $\delta\rho = \dfrac{\rho - P(\rho_{eq})}{P'(\rho_{eq})}$, d'où : $\begin{cases}
				\dfrac1{P'(\rho_{eq})}\partial_t\rho + \rho_{eq}\partial_t u = 0\\
				\rho_{eq} \partial_t u + \partial_x \rho = 0
			\end{cases}$\\
			$\partial_t \begin{bmatrix}
				p\\
				u
			\end{bmatrix} + A\partial_x \begin{bmatrix}
			p \\
			u
			\end{bmatrix} = 0$ avec $A= \begin{bmatrix}
			0 & \rho_{eq}P'(\rho_{eq}) \\
			\dfrac1{\rho_{eq}} & 0
			\end{bmatrix}$ dont les vp sont : $\pm \sqrt{P'(\rho_{eq})} =: \pm c$.
			
			\vspace{.5cm}
			
			\item Le vecteur propre associé à $c$ est $\begin{bmatrix}
				\rho_{eq}\\
				1
			\end{bmatrix}$ et celui associé à $-c$ est $\begin{bmatrix}
			-\rho_{eq}c\\
			1
			\end{bmatrix}$. Ainsi :
			\[
				A = P_AD_AP_A^{-1}\hspace{.5cm} ,\text{avec } D_A = \begin{bmatrix}
					c & 0\\
					0 & -c
				\end{bmatrix} \text{ et } \begin{bmatrix}
				\rho_{eq} c & -\rho_{eq}c \\
				1 & 1
				\end{bmatrix}.
			\]
		\end{minipage}
		\begin{minipage}{17cm}
			Ainsi, puisque $P_A^{-1} = \dfrac{1}{2\rho_{eq}c}\begin{bmatrix}
				1 & \rho_{eq} c \\
				-1 & \rho_{eq} c
			\end{bmatrix}$, pour $W = P_A^{-1} \begin{bmatrix}
			P \\
			u
			\end{bmatrix} = \begin{bmatrix}
			W_+\\
			W_-
			\end{bmatrix}$, il suit :
			\[
				\partial_t W + D_A \partial_x W =0.
			\]
			Ainsi $W_+ = \dfrac1{2\rho_{eq}c}\underbrace{\left(P+\rho_{eq}cu\right)}_{z_+}$ et $W_- = \dfrac{-1}{2\rho_{eq}c}\underbrace{\left(P-\rho_{eq}cu\right)}_{z_-}$ vérifient respectivement :
			\[
				\partial_t W_+ + c \partial_x W_+ = 0\;;\; \partial_t W_- - c \partial_xW_- = 0
			\]
			
			\item $z_+(t,x) = z_+^0(x-ct)$ et $z_-(x,t) = z_-^0(x+ct)$. On a donc :
			\[
				p(t,x) = \frac12 \left(z_+ + z_-\right) = \frac12 \left[p_0(x-ct)+p_0(x+ct)\right]+\dfrac{\rho_{eq}}2\left[u_0(x-ct)-u_0(x+ct)\right]
			\]
			ainsi : $u(t,x) = \dfrac1{2\rho_{eq}c}(z_+-z_-) = \frac12\left[u_0(x-ct)-u_0(x+ct)\right] + \dfrac{1}{2\rho_{eq}c}\left[p_0(x-ct) - p_0(x+ct)\right]$.
			
			\vspace{.5cm}
			
			\item Linéarisation au voisinage d'un équilibre ($\rho_{eq},u_{eq}$) uniforme avec $u_{eq} \not = 0$.\\
			D'après la question 1 : $\begin{cases}
				\partial_t \rho + \partial_x(\rho u) = 0\\
				\partial_t(\rho u) + (c^2-u^2)\partial_x\rho + 2u\partial_x(\rho u) = 0
			\end{cases}$\\
			Ainsi : $\begin{cases}
				\partial_t \rho + u \partial_t \rho + \rho \partial_x u = 0\\
				u \partial_t \rho + \rho \partial_tu + (x^2-u^2)\partial_x \rho + 2u^2\partial_x \rho + 2\rho u \partial_x u = 0
			\end{cases}$\\
			Donc $\dfrac{(2)-u\times(1)}{\rho} = \partial_t u + \dfrac{c^2}{\rho} \partial_x\rho+u\partial_x u = 0$
			
			Ce qui nous donne : $\partial_t \begin{bmatrix}
				\rho \\
				u
			\end{bmatrix} + B \partial_x \begin{bmatrix}
			\rho\\
			u
			\end{bmatrix} = 0$ avec $B = \begin{bmatrix}
			u & \rho \\
			\dfrac{c^2}{\rho} & u
			\end{bmatrix}$\\
			
			\begin{equation}\label{eq:1lin}
				\partial_t\rho + (u_{eq}+\cancel{\delta u})\partial_x(\delta p) + (\rho_{eq} + \cancel{\delta p})\partial_x(\delta u) = 0
			\end{equation}
			, et :\\
			$\partial_t u + \dfrac{c^2}{\rho_{eq}+\delta\rho}\partial_x(\delta\rho) + (u_{eq}+\delta u)\partial_x (\delta u) = 0$.\\
			Ce qui donne : $\partial_t u + \dfrac{c^2}{\rho_{eq}}\partial_x\rho + u_{eq}\partial_x u = 0$ linéarisé pour $\begin{bmatrix}
				\rho \\
				u
			\end{bmatrix}$ : $\partial_t \begin{bmatrix}
			\rho \\
			u
			\end{bmatrix} + B(\rho_{eq},u_{eq})\partial_x\begin{bmatrix}
			\rho\\
			u
			\end{bmatrix}=0$\\
			$P'(\rho_{eq})\ref{eq:1lin} :$ $\begin{cases}
				\partial_t p = u_{eq} \partial_x p + \rho_{eq}c^2 \partial_x u =0\\
				\partial_t u = \dfrac{c^2}{c^2\rho_{eq}}\partial_x p + u_{eq}\partial_x u = 0
			\end{cases}$\\
			$\partial_t \begin{bmatrix}
				p \\
				u
			\end{bmatrix} + (A+u_{eq}I)\partial_x \begin{bmatrix}
			p \\
			u
			\end{bmatrix} = 0$ avec $A = \begin{bmatrix}
			0 & \rho_{eq}c^2\\
			\dfrac1{\rho_{eq}} & 0
			\end{bmatrix}$\\
			$A+u_{eq} I = P_A\begin{bmatrix}
				u_{eq}+c & 0 \\
				0 & u_{eq}-c
			\end{bmatrix}P_A^{-1}$ avec $p(t,x) = \dfrac12\left[p_0(x-(u_{eq}+c)t)+p_0(x-(u_{eq}-c)t)\right]++\dfrac{1}{2\rho_{eq}c}\left[u_0(x-(u_{eq}+c)t)-u_0(x-(u_{eq}-c)t)\right]$.
		\end{minipage}
		\end{enumerate}
	\end{sol}
	
	
	
\end{document}