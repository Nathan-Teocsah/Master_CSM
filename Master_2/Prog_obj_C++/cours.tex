\documentclass[12pt,a4paper]{article}

\makeatletter
\def\input@path{{/home/maenwe/Master_CSM/modèle_cours_latex}}
\makeatother

\usepackage{prise_de_note}

\begin{document}
	\pagenumbering{gobble}
	\begin{titlepage}
		\centering
		
		% Logo
		\begin{figure}[h]
			\centering
			% Ajustez la largeur si besoin (ex: 0.4 ou 0.6)
			\includegraphics[width=0.5\textwidth]{logo.jpg} 
		\end{figure}
		
		\vspace{2cm}
		
		% Filet horizontal du haut
		\rule{\linewidth}{0.5mm} \\[0.4cm]
		
		% Titre
		{\color{bleuTitre} \Huge \bfseries Programmation orienté objet en C++} \\[0.4cm]
		
		% Filet horizontal du bas
		\rule{\linewidth}{0.5mm}
		
		\vspace{1.5cm}
		
		{\Large \textbf{Cours}}
		
		\vfill
		
		% Bloc Informations
		\begin{minipage}{0.45\textwidth}
			\begin{flushleft} \large
				\textbf{Enseignant :} \\
				M. Eric \textsc{DARRIGAND}
			\end{flushleft}
		\end{minipage}
		\hfill
		\begin{minipage}{0.45\textwidth}
			\begin{flushright} \large
				\textbf{Étudiant :} \\
				Nathan \textsc{HASCOET}
			\end{flushright}
		\end{minipage}
		
		\vspace{2cm}
		
		% Date
		{\large \today}
		
	\end{titlepage}
	
	\tableofcontents
	
	\newpage
	
	\pagenumbering{arabic}
	\setcounter{page}{1}
	
	\section{Introduction au C++}
	
	\subsection{Langage compilé}
	
	g++ \begin{minipage}[t]{15cm}
		-C compilation\\
		-std=c++11\\
		-Wall\\
		-fsanitize=address
	\end{minipage}
	
	\underline{Organisation du code}
	\begin{enumerate}[label=-]
		\item type et variables.
		\item fonctions.
		\item \underline{classes}.
		\item espaces de noms (\textit{namespace}).
	\end{enumerate}
	(std::)sqrt\\
	$\uparrow$\\
	namespace de la lib standard (STL).\\
	
	La STL contient : \begin{minipage}[t]{15cm}
		\textit{\#include} \begin{minipage}[t]{15cm}
			\textit{<cmath>}\\
			\textit{<cstdlib>}
		\end{minipage}
		
		using std::sqrt;
	\end{minipage}
	
	\subsection{Entrée/Sorties standard}
	En C++, on passe par les flots : "\textit{cin}", "\textit{cout}", "$>>$" et "$<<$".
	

	\begin{lstlisting}[language=C++, caption=Exemple de code en C++]
		#include <iostream>
		using namespace std;
		int main(){
			int num_annee;
			cout << "Donner le num de l annee"" << endl;
			cin >> num_annee;
			cout<<"Bonjour et bonne rentreee" << num_annee << endl;
		}
	\end{lstlisting}
	
	\newpage

	\subsection{Notion de classe en C++}
	
	Définition de base d'une classe :
	
	\begin{lstlisting}[language=C++, caption=Définition d'une classe]
		class MaClasse{
			private :
				// donnees membres
				int donnee 1;
				int donnee 2;
			public :
				//constructeurs (optionnels).
				MaClasse(...);
				// fonctions membres ou methodes
				int fonc1();
				double fonc2(int);
			}
	\end{lstlisting}
	
	Les fonctions membres peuvent être définies ailleurs.
	
	\begin{lstlisting}[language=C++, caption=Définition d'une classe]
		MaClasse::MaClasse(...){...}
		int MaClasse::fonc1(){...}
		double MaClasse::fonc2(int i){...}
	\end{lstlisting}
	
	\noindent\underline{Usage :} dans un programme appelant.\\
	\hspace*{1.5cm}\begin{minipage}{15cm}
		MaClasse obj1; \hspace{1cm} cout $<<$obj1\\
		MaClasse* obj1\_p; \hspace{1cm} cout $<<$obj
	
	\end{minipage}
	
	\subsection{Structuration d'un code en fichiers}
	
	\begin{enumerate}[label=$\to$]
		\item Des fichiers .hpp (ou .h++ ou .h)\\
		Ce sont des headers (essentiellement des déclarations)\\
		$\hookrightarrow$ définitions de classes \begin{minipage}[t]{7cm}
			\begin{lstlisting}[language=C++,caption=Exemple de header]
				#ifndef MACLASSE_H
				#define MACLASSE_H
				class MaClass{
					...
				}
				#endif //MACLASSE_H
			\end{lstlisting}
		\end{minipage}
		\item Des fichiers .cpp (en .c++)\\
		$\hookrightarrow$ programmation des foncions déclarées et non définies dans les hpp.
	\end{enumerate}
	
	\newpage
	
	\subsection{Premières lignes d'un .cpp}
	
	\begin{lstlisting}[language=C++,caption=Début d'un .cpp]
		#include "MesClasses.hpp"
		#include <iostream>
		#include <cmath>
		using std::cin;
		using std::sqrt;
	\end{lstlisting}
	
	\subsection{Exemple de classe}
	
	La classe complexe.\\
	"Complexe".
	\begin{enumerate}[label=$\to$]
		\item 2 données membres\\
		partie réelle (double)\\
		partie imaginaire (double)
		
		\item construction avec 2 arguments d'entrées de type double pour définir les données membres
	\end{enumerate}
	
	\begin{lstlisting}[language=C++,caption=complexe.hpp]
		#ifndef COMPLEXE_H
		#define COMPLEXE_H
		class Complexe{
			private:
				double re, im;
			public :
				Complexe(double a, double b);
		};
		#endif // COMPLEXE_H
	\end{lstlisting}
	\begin{lstlisting}[language=C++,caption=complexe.cpp]
		#include "complexe.hpp"
		
		Complexe::Complexe(double a,double b){
			re = a;
			im = b;
		}
	\end{lstlisting}
	
	\noindent Ces exemples et d'autre se trouve dans le livre que l'on peut récupérer à l'adresse suivante :
	\begin{center}
		\url{https://www.stroustrup.com/}
	\end{center}
	
	Des classe à metre de préférence dans le .hpp :
	\begin{enumerate}[label=$\star$]
		\item Définitions de types, classes, déclarations de classes.
		\item Déclarations de fonctions.
		\item Déclarations de variables globales
		\item Définitions de variables consantes.
		\item Définitions des fonctions "inline" (à réserver aux fonctions courtes).
	\end{enumerate}
	\begin{lstlisting}[language=C++,caption=Exemple de fonction "inline" (complexe.hpp)]
		#ifndef COMPLEXE_H
		#define COMPLEXE_H
		class Complexe{
			private:
				double re, im;
			public :
				Complexe(double a, double b){
					re = a;
					im = b;
				} // fonction "inline"
		};
		#endif // COMPLEXE_H
	\end{lstlisting}
	
	Le pré-processeur va remplacer à chaque occurrence la fonction Complexe par le script qui est donné. Ce qui peut rendre le code très lourd à exécuter.\\ 
	
	Dans une .hpp, on évitera :
	\begin{enumerate}[label=$\star$]
		\item Définition d'une variable non constante.
		\item Usage de la directive "using" (interdit : \textit{stroustrup} [2014] p.424).
	\end{enumerate}
	
	\begin{lstlisting}[language=C++,caption=Exemple d'un main.cpp]
		#include "complexe.hpp"
		int main(){
			Complexe z1(1.5,2.5);
			Complexe z2;
		}
	\end{lstlisting}
	
	\vspace{-.5cm}
	
	\subsection{Arguments avec valeur par défaut dans une fonction}
	
	\begin{lstlisting}[language=C++,caption=Exemple 1]
		int sum(int a, int b = 1){return a+b;}
		int a=2,b=4,c,d,e;
		c = som(a); //  c=3
		d = sum(b); // d=5
		e = sum(a,b); // e=6
	\end{lstlisting}
	
	\begin{lstlisting}[language=C++,caption=Exemple 2]
		int sum(int a=0, int b = 1){return a+b;}
		int a=2,b=4,c,d,e,f;
		c = som(a); //  c=3
		d = sum(b); // d=5
		e = sum(a,b); // e=6
		f = sum(); // f=1
	\end{lstlisting}
	
	\subsection{Surcharge de fonctions}
	
	\begin{lstlisting}[language=C++,caption=Exemple de surcharge de fonctions]
		int add(int a, int b){return a+b};
		double add(double a,double b){return a+b};
		int a=0,b=1,e;
		double c=0.5,d=1.5,f,g;
		e = add(a,b); // e=1
		f = add(c,d); // f=2
		g = add(c,b); // crash : refus par le compilateur (ambigu)
	\end{lstlisting}
	
	\subsection{Transmission par valeurs, par pointeurs ou par références}
	
	\begin{lstlisting}[language=C++,caption=Transmission par pointeur]
		int a=2;
		int* a_p = &a;
		carre(a_p);
	\end{lstlisting}
	
	Une référence est un "alias" vers une variable déjà déclaré : 
	\begin{enumerate}[label=$\to$]
		\item L'accès à la référence est semblable à l'accès à la variable elle-même.
		\item Une référence réfère toujours à l'objet auquel elle a été initialisée.
		\item Une référence est nécessairement rattachée à un objet.
	\end{enumerate}
	
	\begin{lstlisting}[language=C++,caption=Exemple d'une référence]
		int var=1;
		int& r(var); //r est une reference sur la variable "var".
		int x=r; // x prend la valeur 1.
		r=2; // la valeur de var devient 2 mais x ne change pas.
		r++; // la valeur de var devient 3.
		int* p = &r; // p pointe sur la variable var.
	\end{lstlisting}
	\begin{lstlisting}[language=C++,caption=Transmission par référence]
		int add(int a, int b){return a+b};
		int add(int* a_p,int* b_p){return *a_p+*b_p};
		int add(int& a, int& b){return a+b};
		int carre(int& a, int& b){return a*=a};
	\end{lstlisting}
	
	La première et la troisième fonction ne peuvent pas cohabiter dans le même script, car le programme ne saura pas dire lequel utiliser puisque la référence peut-être utilisée par les deux fonctions.
	
	\newpage
	
	\subsection{Allocation dynamique}
	
	\textit{new} et \textit{delete}
	
	\begin{lstlisting}[language=C++,caption=Syntaxe]
		double* d = new double(5.1);
		delete d;
	\end{lstlisting}
	
	$d$ est un pointeur sur un double.
	
	\subsection{Constructeurs avec valeurs d'initialisation par défaut, liste d'initialisation}
	
	\begin{lstlisting}[language=C++,caption=Exemple]
		class Point{
			private:
				double xP, yP;
			public:
				Point(double x, double y):xP(x),yP(y){
					cout << "construction double double" << endl;
					cout << "Objet " << this << endl;
				}
		}
	\end{lstlisting}

	\subsection{Constructeurs par défaut, par copie}
	
	Par exemple poir la classe Complexe.
		
	\begin{lstlisting}[language=C++,caption=Constructeur par defaut et par copie.]
		class Complexe{
			public :
				Complexe(); // constructeur par defaut
				Complexe(const Complexe& z){re = z.re; im = z.im}
		}
	\end{lstlisting}
	
	\begin{lstlisting}[language=C++,caption=Exemple d'utilisation.]
		Complexe z; // constructeur par defaut de complexe
		Complexe z1(z), // consructeur par copie
		Complexe z2(0,1.5); //constructeur d'un complexe par "double,double" avec copies des 2 "double"
		Complexe z3=z2; //Constructeur par copie, equivalent a Complexe z3(z2)
		Complexe z4; z4 = z3; // constructeur par defaut puis affectation.
	\end{lstlisting}
	
	\subsection{Destructeur}
	
	Le prototype destructeur de MaClasse est :
	\begin{lstlisting}[language=C++,caption=Prototype du destructeur.]
		~MaClasse();
	\end{lstlisting}
	Il est possible de le redéfinir :
	\begin{lstlisting}[language=C++,caption=Exemple d'utilisation.]
		class Point{
			public:
				Point(){...}
				Point(...,...);
				~Point(){cout<<"destruction d'un point : "<<this<<endl;}
		}
	\end{lstlisting}
	
	\subsection{Pointeur this}
	Accessible au sens des foncions memnres de la classe, c'est un pointeur vers l'objet instancié.
	
	\subsection{Données membres et fonctions membres statique}
	
	
	\begin{lstlisting}[language=C++,caption=Exemple d'utilisation.]
		class Point{
			static Point PointRef;
			
			static void afficheRef();
		}
	\end{lstlisting}
	
	\begin{lstlisting}[language=C++,caption=Illustration.]
		Point P1(1,0);
		Point P2(0,1);
		
		P1.afficheRef(); //Ce qui suit est la meme chose que cette ligne
		P2.afficheRef();
		Point::afficheRef();
	\end{lstlisting}
	
	\textcolor{red}{\fbox{\textbf{!}}} Le pointeur "this" n'est pas accesssible dans une fonction membre déclarée static.
	
	
\end{document}